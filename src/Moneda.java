public record Moneda(String result,
                     String conversion_rate,
                     int conversion_result,
                     String base_code,
                     String target_code) {
}
